{
  "name": "rock-virtual-assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "build:data": "node scripts/build-data.mjs",
    "dev": "node scripts/server.mjs",
    "build": "npm run build:data",
    "start": "node scripts/server.mjs",
    "tunnel": "lt --port 3000",
    "smoke": "node scripts/smoke-test.mjs"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "localtunnel": "^2.0.2"
  }
}
